Correção v6

Erro corrigido:
- RuntimeState::update_frame estava chamando self.update(...), mas esse método não existe.

Ajuste aplicado:
- update_frame agora executa o fluxo básico real do runtime:
  - atualiza frame_count
  - calcula delta_time
  - acumula elapsed_time
  - aplica mudança de cena pendente
  - processa spawns pendentes
  - processa destruições pendentes
  - atualiza partículas
  - roda run_runtime_systems na cena ativa

Os warnings de imports não usados continuam não impedindo compilação.
