// Script básico para Rust2D Engine
// start() é chamado ao iniciar
// update(delta) é chamado a cada frame

// @start_message Novo script pronto!

pub struct NovoScript;

impl NovoScript {
    pub fn start(&mut self) {
        // Inicialização
    }
    pub fn update(&mut self, delta: f32) {
        // Lógica por frame
    }
}
