// Funções de desenho do runtime.
// Este arquivo passa a ser o lugar correto para código visual específico do runtime.
// Mantido simples nesta etapa para evitar duplicação em runtime/mod.rs.

pub fn module_note() -> &'static str {
    "Mover gradualmente o desenho do runtime para este módulo."
}
