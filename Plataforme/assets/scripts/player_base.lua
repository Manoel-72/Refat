-- Script Lua — RS2BR-Engine V0.9.0
--
-- Estrutura mínima recomendada.
-- Use on_start para inicialização e on_update(dt) para lógica por frame.
--

function on_start()
end

function on_update(dt)
end
